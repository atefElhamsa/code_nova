---
id: intro-to-mvvm
title: "4.1 Introduction to MVVM"
sidebar_label: "4.1 Introduction to MVVM"
---

import ExerciseBox from '@site/src/components/ExerciseBox';
import SummaryBox from '@site/src/components/SummaryBox';

# 4.1 مقدمة إلى معمارية MVVM (Intro to MVVM)

تُعتبر معمارية MVVM (Model-View-ViewModel) النمط المعماري الأساسي الموصى به لتطوير تطبيقات Flutter النظيفة والقابلة للاختبار.

---

##  أهداف الدرس (Learning Objectives)

1. فهم الأدوار الثلاثية: Model, View, و ViewModel.
2. فصل منطق عرض الواجهة عن رسم الودجات.
3. تحديد تدفق البيانات ثنائي الاتجاه.

---

## 1. النموذج (Model)

### 📘 الشرح النظري
يمثل الـ Model كائنات البيانات النقية التي تستقبل من السيرفر أو قاعدة البيانات (Data Structure) بدون أي كود Flutter.

### 💻 التطبيق العملي
```dart
class UserEntity {
  final String id;
  final String name;
  const UserEntity({required this.id, required this.name});
}
```

---

## 2. نموذج العرض (ViewModel)

### 📘 الشرح النظري
يتولى الـ ViewModel استقبال طلبات الواجهة، التواصل مع الـ Repository، وإخطار الـ View عند تغير الحالة عبر `notifyListeners()`.

### 💻 التطبيق العملي
```dart
class UserViewModel extends ChangeNotifier {
  String _name = 'Guest';
  String get name => _name;

  void updateName(String newName) {
    _name = newName;
    notifyListeners(); // إخطار الواجهة بالتغيير
  }
}
```

---

## 3. الواجهة (View)

### 📘 الشرح النظري
تقتصر وظيفة الـ View على عرض العناصر البصرية والاستماع لتغيرات الـ ViewModel دون احتواء أي منطق برمجي.

### 💻 التطبيق العملي
```dart
class UserView extends StatelessWidget {
  const UserView({super.key});

  @override
  Widget build(BuildContext context) {
    return Consumer<UserViewModel>(
      builder: (context, vm, child) => Text(vm.name),
    );
  }
}
```

---

##  نصائح الأداء والتحسين (Performance Tips)

:::tip فصل تام بين UI و State
عدم كتابة أي دالة HTTP أو Database داخل الـ Widget نهائياً.
:::

---

##  أخطاء شائعة يجب تجنبها (Common Mistakes)

```dart
// ❌ 1. إنشاء كائن الـ Repository داخل طريقة build() للودجة
Widget build(BuildContext c) { final repo = Repository(); ... }

// ✅ الحل الصحيح
Widget build(BuildContext c) { final repo = context.read<Repository>(); ... }

```

---

##  أسئلة وأجوبة المقابلات (Interview Q&A)

<details>
<summary><strong>س1: ما هو الدور الرئيسي للـ ViewModel في نمط MVVM؟</strong></summary>

إدارة حالة الشاشة، تحويل البيانات للعرض، واستقبال أحداث المستخدم دون الاعتماد على ودجات Flutter.

</details>

---

<ExerciseBox title="تمرين عملي - تطبيق نمط MVVM 🏋️">

أنشئ Model و ViewModel و View بسيطة لاستعراض نص وترجمته.

</ExerciseBox>

<details>
<summary><strong>💡 اضغط هنا لرؤية الحل النموذجي المشروح بالتعليقات (Solution Code)</strong></summary>

```dart
class SimpleViewModel extends ChangeNotifier {
  String text = 'Hello';
  void change() { text = 'World'; notifyListeners(); }
}
```

</details>

---

<SummaryBox title="ملخص الدرس 📌">

- Model للبيانات النقية.
- ViewModel لإدارة الحالة وإخطار الواجهة.
- View لرسم الودجات فقط.

</SummaryBox>
