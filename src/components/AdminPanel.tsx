import React from 'react';

// AdminPanel is now fully self-contained inside admin.tsx
// This file is kept as a placeholder
export default function AdminPanel({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
