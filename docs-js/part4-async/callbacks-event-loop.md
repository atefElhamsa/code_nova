---
sidebar_position: 1
---

# Callbacks & Event Loop

## 🎯 Learning Objectives
- فهم طبيعة جافاسكريبت بأنها أحادية المسار (Single-threaded).
- معرفة كيفية تعامل المتصفح مع المهام عبر الـ Event Loop.
- استخدام دالة الرد (Callback Function) لتنفيذ أكواد غير متزامنة.

---

## 1. هل جافاسكريبت أحادية المسار؟ (Single-threaded)

نعم! جافاسكريبت تنفذ الأوامر سطراً بسطر في مسار واحد (Thread) فقط. يعني هذا نظرياً أنها لا تستطيع فعل شيئين في نفس الوقت.

ولكن، كيف نقوم بجلب بيانات من الإنترنت ونحن نتفاعل مع الصفحة بدون أن يتجمد المتصفح؟ 
هنا يأتي دور **الـ Event Loop** والـ **Web APIs**.

> 💡 **مقارنة بـ Dart:**
> Dart أيضاً Single-threaded وتعتمد على مفهوم الـ `Event Loop` لتشغيل الكود غير المتزامن (Asynchronous Code) تماماً مثل جافاسكريبت! المفهوم الهندسي هنا متطابق بنسبة كبيرة.

## 2. ما هو الـ Event Loop؟

باختصار: جافاسكريبت تقوم بتنفيذ الأكواد المتزامنة (التي تنتهي فوراً) أولاً. أي كود يحتاج لوقت (مثل جلب بيانات أو مؤقت زمني) يتم إرساله إلى المتصفح (Web API) ليقوم به في الخلفية. 
وعندما ينتهي المتصفح من المهمة، يضع "دالة الرد" في طابور (Queue)، ثم يقوم الـ Event Loop بدفعها لجافاسكريبت لتنفيذها بمجرد أن تكون فارغة.

## 3. دوال الرد (Callbacks)

دالة الرد (Callback) هي ببساطة: "دالة تمررها كمتغير لدالة أخرى، ليتم استدعاؤها لاحقاً".

```javascript
console.log("1. Start");

// setTimeout من دوال المتصفح وتأخذ Callback
setTimeout(() => {
  console.log("2. Data loaded from database");
}, 2000); // تأخير ثانيتين

console.log("3. End");

/* المخرجات ستكون:
1. Start
3. End
2. Data loaded from database (بعد ثانيتين)
*/
```

### مشكلة الـ Callback Hell
إذا كان لديك عدة عمليات غير متزامنة تعتمد على بعضها (مثلاً تجلب مستخدم، ثم تجلب منشوراته، ثم تجلب تعليقاته)، ستضطر لوضع Callbacks داخل بعضها البعض، مما ينتج عنه كود متداخل بشكل مخيف يسمى (Callback Hell أو هرم الموت).

```javascript
// مثال نظري للـ Callback Hell
getUser(1, (user) => {
  getPosts(user.id, (posts) => {
    getComments(posts[0].id, (comments) => {
      console.log(comments);
    });
  });
});
```

لحل هذه المشكلة، ظهرت الـ Promises كما سنرى في الدرس القادم!

---

## 💻 جرب بنفسك (Hands-on)
1. قم بكتابة أمر `console.log('A')`.
2. استخدم `setTimeout` لطباعة `'B'` بعد 1000 جزء من الثانية (ثانية واحدة).
3. اكتب أمر `console.log('C')` في النهاية.
4. توقع ترتيب الطباعة، ثم شغل الكود ولاحظ كيف أن C تسبق B.

---

<div className="summary-box">
  <h3>📝 خلاصة</h3>
  <ul>
    <li>جافاسكريبت <code>Single-threaded</code> مثل Dart، لكنها لا تتوقف بفضل الـ <code>Event Loop</code>.</li>
    <li>الـ <code>Callback</code> هو تمرير دالة ليتم تنفيذها في وقت لاحق عند انتهاء المهمة.</li>
    <li>الاستخدام المفرط للـ Callbacks المتداخلة يؤدي لـ <code>Callback Hell</code>.</li>
  </ul>
</div>
